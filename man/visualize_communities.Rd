% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vizualize_communities.R
\name{visualize_communities}
\alias{visualize_communities}
\title{Visualize HEMA Communities}
\usage{
visualize_communities(
  graph,
  color_palette = NULL,
  layout_type = "fr",
  weight_threshold_dotted = 5,
  show_labels = TRUE
)
}
\arguments{
\item{graph}{A `tbl_graph` object from the `tidygraph` package, representing nodes and edges of the graph.}

\item{color_palette}{A character vector specifying custom colors for communities. If `NULL`, a default palette is used.}

\item{layout_type}{A character string specifying the layout algorithm. Options include `"fr"` (default), `"kk"`, `"mds"`, and `"circle"`.}

\item{weight_threshold_dotted}{A numeric value specifying the threshold for dotted line edges. Default: 5.}

\item{show_labels}{boolean - turn off the grapgh description.}
}
\value{
A ggplot object representing the visualized graph.
}
\description{
Generates a visual representation of a graph object, applying community detection and customizable layouts.
Filters edges by weight and applies a color palette to distinguish communities. Provides an aesthetically styled plot.
}
\examples{
\dontrun{
# Example 1: Basic graph visualization with default settings
library(dplyr)
generate_communities_graph( tournament_weapon = "Steel Longsword", 
                            year=2024,
                           weight_threshold = 5, 
                           population_threshold = 15)\%>\%
visualize_communities()

# Example 2: Custom layout and color palette
custom_palette <- c("#1B9E77", "#D95F02", "#7570B3")
visualize_communities(graph, year = 2024, color_palette = custom_palette, layout_type = "kk")
}

}
